Description:
    Copies migrations into the host application

Example:
    bin/rails generate nla:blacklight_common:install

    This will copy migrations that don't already exist into the `db/patrons_migrate` directory
